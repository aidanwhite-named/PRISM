# 검색 코드 복원 — 2026-09-14

사용자 요청에 따라 검색 실행을 2026-09-06 커밋 `25fff6e` 기준으로 복원했다.

- 검색 런타임 프롬프트, agy 도구 감사·스트림 처리, 결과 파서, 후속 검색, MCP 검색, EPO 조회, 검증·보고서 처리를 복원했다.
- 최근 추가한 검색 성공 질의 검사와 과거 웹 실패 기록에 의한 실행·preflight 차단을 제거했다.
- 이번 진단 중 추가한 agy 1.2.2 호환 프록시, grep_search 제한 허용, 출력 JSON 보정을 제거했다. grep_search는 기준 커밋과 동일하게 허용되지 않는다.
- 이후 추가된 검색 recall·efficiency 기능과 해당 전용 테스트를 함께 제거하고, 기존 검색 테스트를 기준 커밋으로 복원했다.
- 설정 화면의 독립적인 검색 도구 진단, 브라우저 로그인, 정답 사례 기능은 유지했다. 진단 기록은 검색 실행을 막지 않는다.
- agy 바이너리·버전은 변경하지 않았다. 기존 자동 업데이트 방지 설정은 유지하여 사용자가 선택한 버전을 보존한다.
- 별도로 수정 중인 프런트엔드 파일은 변경하지 않았다.

복원 직전 파일과 제거한 파일의 백업:
`C:\Users\Administrator\AppData\Local\PRISM\diagnostics\search-rollback-20260914-133001`

이 복원은 코드의 과거 동작을 재현한다. 현재 agy 서비스의 실제 검색 성공을 보증하지 않으며, 실검색은 사용자 측 버전 조정 후 확인해야 한다. 이전 진단 문서에 기록된 호환 프록시와 grep 허용은 현재 적용되어 있지 않다.

검증: 검색·Provider 관련 234개, 검색 입력·EPO·로그인·정답 사례 관련 272개 테스트 통과(총 506개). 복원한 10개 검색 모듈과 검색 설정은 기준 커밋과 AST 비교로 일치 확인. 실행 중 작업이 없는 것을 확인한 뒤 로컬 서버를 재시작했고 `/api/health` 정상 응답을 확인했다.
